function even_or_odd(number) {
  let result;
  if (number % 2 === 0) {
    result = 'Even';
  } else {
    result = 'Odd';
    }
return result
}

//'else' is the fallback, does not require a condition